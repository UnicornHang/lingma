# ZhiMeng Frontend - Development Environment
#
# VITE_API_BASE / VITE_WS_BASE 都是「在前端代码中前缀拼接」的额外路径。
# dev 下 Vite 已经把 /api、/ws 代理到 localhost:8000,所以这里都留空:
#   - 前端 HTTP 请求:  ${''}/api/v1/...  →  /api/v1/...
#   - 前端 WS 连接:    ws://localhost:5173${''}/ws/generation/...  →  /ws/generation/...
#
# ⚠️ 常见错误:把 VITE_WS_BASE 写成 "/ws",会变成 "/ws/ws/generation/..."(双前缀 → 403)
#
# 生产环境(nginx 同域反代)下,可改成具体前缀,例如:
#   VITE_API_BASE=/api/v1
#   VITE_WS_BASE=
VITE_API_BASE=/api/v1
VITE_WS_BASE=
VITE_APP_VERSION=0.1.0